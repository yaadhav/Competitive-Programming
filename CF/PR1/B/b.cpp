#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);

    int t, cases, n, i, j;
    vector<int> s; int ts;

    cin >> t ;

    for( cases=0; cases<t; cases++)
    {
        cin >> n ;

        for( i=0; i<n; i++)
        {
            cin >> ts;
            s.push_back(ts) ;
        }

        

        
    }

    cin.tie(NULL);
    return 0;
}

#include<bits/stdc++.h>
using namespace std;

typedef long long int ll;

void recurse( int ind, int x, vector<int> arr) {

    if( ind==-1 ) re
}

int main()
{
    ll t;
    cin >> t;

    while(t--)
    {
        ll x;
        cin >> x;

        vector<int> ans;

        recurse( 0, x, ans);


        
    }

    return 0;
}
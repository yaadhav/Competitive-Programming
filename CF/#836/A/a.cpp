#include <bits/stdc++.h>
#include <string>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    
    int t, cases, i, j;
    vector<string> str ;

    cin >> t ;

    for( cases=0; cases<t; cases++)
    {
        cin >> str[0]
    }   
    cin.tie(NULL);
    return 0;
}

#include <bits/stdc++.h>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t ;

    while(t--)
    {
        int n, m;
        cin >> n >> m;

        vector<int> p, a;
        int temp;

        for( int i=0; i<m; i++)
        {
            cin >> temp ;
            p.push_back(temp);
        }

    } 
    
    return 0;
}


#include <bits/stdc++.h>
#include <vector>
using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);

    long long int t , cases, i;
    //long long int i;
    vector<long long int> a; int v;

    cin >> t ;

    for( cases=0; cases<t; cases++ )
    {
        cin >> v;
        a.push_back(v) ;
    }



    

    cin.tie(NULL);
    return 0;
}
